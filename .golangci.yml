version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gosec
  settings:
    gosec:
      excludes:
        - G304   # file path from variable — expected in vault/config code

issues:
  exclude-dirs:
    - apps/slm-engine
