linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec

linters-settings:
  gosec:
    excludes:
      - G304   # file path from variable — expected in vault/config code

issues:
  exclude-dirs:
    - apps/slm-engine
