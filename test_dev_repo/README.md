# Developer Setup

> **Synthetic test fixture for OCULTAR's PII/secrets-detection engine.** Every credential, key, and password below is fabricated by `scripts/generate_dev_repo.py` and contains no real access to any system. Nothing here is a live leak.

Welcome to the project. To access the staging server, run:
```bash
ssh dev-user@ssh.staging.company.net
```
The password is `staging_secret_pass`.
