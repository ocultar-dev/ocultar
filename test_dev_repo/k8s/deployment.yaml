apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
spec:
  template:
    spec:
      containers:
      - name: api
        env:
        - name: GITHUB_TOKEN
          value: "ghp_secure_github_token_992182bbcca382"
        - name: DB_HOST
          value: "db-replica-01.database.svc.cluster.local"
